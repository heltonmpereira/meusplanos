@model MeusPlanos.AppCliente.ViewModel.WeatherForecastViewModel

@{
    ViewData["Title"] = "Temperaturas";
    ViewData["Icone"] = "<i class='fa fa-thermometer fa-lg'></i>";
    @section Scripts {
        @{
            await Html.RenderPartialAsync("_ValidationScriptsPartial");
        }
    }
}

<form asp-action="Alterar" method="post" class="col-11 px-sm-3 px-md-0 pt-4 mx-auto">
    <h1>Alterar</h1>
    <hr />

    @Html.EditorForModel()
</form>